name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # Every top-level directory holding a Makefile is a project. Discovering them
  # here means adding a project needs no CI edits -- it just has to follow the
  # standard layout documented in AGENTS.md.
  discover:
    # Pull requests execute repository code, so keep this on an isolated,
    # preferably ephemeral runner with no refresh-bot secrets.
    runs-on: [self-hosted, static-sites-ci]
    outputs:
      projects: ${{ steps.list.outputs.projects }}
    steps:
      - uses: actions/checkout@v4
      - id: list
        run: |
          projects=$(ls -d */Makefile | xargs -n1 dirname | jq -R -s -c 'split("\n")[:-1]')
          echo "projects=$projects" >> "$GITHUB_OUTPUT"
          echo "Discovered: $projects"

  build:
    needs: discover
    runs-on: [self-hosted, static-sites-ci]
    strategy:
      fail-fast: false
      matrix:
        project: ${{ fromJson(needs.discover.outputs.projects) }}
    name: ${{ matrix.project }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: ${{ matrix.project }}/uv.lock
      # Both targets are contractually offline (see AGENTS.md): anything needing
      # the network lives behind `make data`, which CI never runs.
      - name: make test
        run: make -C ${{ matrix.project }} test
      - name: make all
        run: make -C ${{ matrix.project }} all

  # Validate repo-admin metadata and its stdlib-only scheduler tests. No
  # uv/venv setup is needed.
  manifest:
    runs-on: [self-hosted, static-sites-ci]
    steps:
      - uses: actions/checkout@v4
      - name: Repo metadata is valid
        run: make check
